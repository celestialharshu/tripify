// small reusable spinner shown while we wait on API calls
const Loader = () => {
  return (
    <div className="loader-wrap">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
